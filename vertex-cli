#!/bin/bash

# Vertex CLI launcher script
# This script provides a convenient way to run vertex-cli from the repo root

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec "$SCRIPT_DIR/bin/vertex-cli" "$@"
